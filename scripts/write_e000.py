# -*- coding: utf-8 -*-
"""把 E-000 的结果写进 EXPERIMENTS.md。"""
import io
import shutil

P = "/root/autodl-tmp/slt/docs/EXPERIMENTS.md"
s = io.open(P, encoding="utf-8").read()

OLD_ROW = """| 实验编号 | 阶段 | 改了什么 | BLEU-4 | ROUGE | 训练耗时 | 备注 |
|---------|------|---------|--------|-------|---------|------|
| — | — | *（尚无实验，数据预处理进行中）* | — | — | — | — |"""

NEW_ROW = """| 实验编号 | 阶段 | 改了什么 | BLEU-4 | ROUGE | 训练耗时 | 备注 |
|---------|------|---------|--------|-------|---------|------|
| **E-000** | 0 | 老架构 baseline：逐帧 MLP（不跨帧）+ BiLSTM 编码器 + 带 Bahdanau attention 的 LSTM 解码器；输入 pose 538 维 | **1.41 ± 0.15** | ROUGE-L **20.86 ± 0.69**（R-1 21.81 / R-2 3.08） | 40.6 分钟/趟 × 3 seed | 3 seed 均值±std；lr=3e-4 epochs=100 batch=16；signer-dependent 官方划分 |

⚠️ **E-000 有一处方法论缺口**：选中的 lr=3e-4 是搜索范围的**下边界**，
最优值可能在范围之外（见下方实验日志与 DECISIONS.md D-018）。"""

assert s.count(OLD_ROW) == 1, "占位行未唯一命中"
s = s.replace(OLD_ROW, NEW_ROW)

OLD_DETAIL = "### E-000（未开始）\n"
NEW_DETAIL = """### E-000 阶段 0 baseline（2026-09-15 完成）

**配置**：lr=3e-4，epochs=100，batch=16，frame_stride=2，max_frames=256，
normalize=body，字级词表 1974（仅由 train.csv 构建），模型 20.09M 参数。
train 4973 训练 / dev 515 选 checkpoint / test 500 只评一次。

**lr 搜索（seed 固定 1234，按 dev 最佳 BLEU-4 选）**

| lr | dev 最佳 BLEU-4 |
|----|---------------:|
| **3e-4** | **2.18** ← 选中 |
| 1e-3 | 1.17 |
| 3e-3 | 0.84 |

**三个 seed 的 test 结果**

| seed | test BLEU-4 | test ROUGE-L | 选点时 dev BLEU-4 | 选中的 epoch | 训练耗时 |
|------|------------:|-------------:|-----------------:|------------:|--------:|
| 1234 | 1.55 | 21.83 | 2.18 | 24 | 40.6 min |
| 2345 | 1.48 | 20.26 | 2.37 | 19 | 40.5 min |
| 3456 | 1.19 | 20.48 | 2.12 | 38 | 40.5 min |
| **均值±std** | **1.41 ± 0.15** | **20.86 ± 0.69** | | | |

**test 按手语者分层（3 seed 均值）**

| T | n | BLEU-4 | ROUGE-L | | T | n | BLEU-4 | ROUGE-L |
|---|--:|-------:|--------:|-|---|--:|-------:|--------:|
| A | 55 | 0.91 | 14.95 | | G | 56 | 0.63 | 17.39 |
| B | 57 | 1.40 | 22.40 | | H | 9 | 1.88 | 16.46 |
| C | 42 | 0.97 | 13.10 | | I | 9 | 1.37 | 10.02 |
| D | 47 | **3.66** | 25.53 | | J | 38 | 2.76 | 26.80 |
| E | 54 | 2.16 | 26.78 | | K | 46 | 0.67 | 20.00 |
| F | 58 | **0.69** | 22.69 | | L | 29 | 0.89 | 22.98 |

### 曲线观察与诊断

**1. 严重过拟合，dev 早早见顶。**
训练 loss 从 5.10 一路降到 **0.285**，而 dev BLEU-4 在 **epoch 24 见顶 2.18**，
之后回落并稳定在 1.76 不动。三个 seed 选中的 epoch 分别是 24 / 19 / 38。

→ epochs=100 **远非欠训练，而是过训练**。D-017 风险栏里"若 100 轮时曲线还在
上升则需加 epoch"的担心可以排除，方向正好相反。靠 dev 选 checkpoint 兜住了。

**2. 不是模式塌缩，输出是多样的。**
test 500 条预测里有 **423 / 404 / 448 种**不同句子（三个 seed）。
模型生成的是**通顺但与视频内容无关**的中文：

| 预测 | 参考 |
|------|------|
| 二氧化碳转化为汽油技术的发展还可以激发更多的创新。 | 40平米的房子比较适合… |
| 人民医院怎么走？ | 下雨后空气会变好。 |
| 不要在电梯捣乱。 | 一年之内这个优惠券都可以用。 |

模型学到的主要是**中文语言模型**，而不是"视频→句子"的映射。
（视觉通路本身是通的，已由 D-015 的全零/错配消融证实。）

**3. BLEU 与手部检出率不相关。**
F 的手部检出最好（0.993/0.977）但 BLEU 最低（**0.69**）；
D 的检出一般（0.677/0.418）反而 BLEU 最高（**3.66**）。
与"模型主要靠语言先验生成"一致 —— 视觉信号还没被用到能让检出质量
产生影响的程度。

**4. 这正是待消融的缺陷本身。**
CLAUDE.md 缺陷 #3「LSTM 解码器无语言先验，小数据量学不动目标语言语法」
在这里表现为：解码器确实学到了语言规律，但因为没有预训练语言先验，
它只能靠死记训练集来获得流畅度，于是在 4973 条上迅速过拟合。
这是阶段 2（换 LLM 解码器）要解决的问题。

### 已知缺口（写入 DECISIONS.md D-018）

- **lr 搜索触到下边界**：3e-4 是搜索范围里最小的值且它胜出，趋势指向更小的 lr。
  最优值可能在范围之外，当前 baseline 可能被低估。
- 未调 dropout（固定 0.3）、未调模型规模、未调 batch。20.09M 参数对 4973 条
  样本偏大，过拟合可能与容量有关，但未做对照。

"""
assert s.count(OLD_DETAIL) == 1, "E-000 明细占位未唯一命中"
s = s.replace(OLD_DETAIL, NEW_DETAIL)

shutil.copy(P, P + ".bak")
io.open(P, "w", encoding="utf-8").write(s)
print("EXPERIMENTS.md 已写入 E-000")
