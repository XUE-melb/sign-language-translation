# 实验记录

每次训练 + 评测追加一行。**这张表的最终形态就是四阶段消融表，是本项目的核心产出。**

## 评估纪律（每次填表前复读）

- 只用标准划分的 **test**（500 条），严禁拿训练分布内样本做演示
- **本项目是 signer-dependent 评估**：官方划分里 12 个 signer 在 train/dev/test
  全部出现，test 中训练未见过的人为 0/12（见 DECISIONS.md D-009）。
  **每次报告数字都要带上这个前提**，分数天然偏乐观。
- 评测输出必须**同时给出按 Translator 聚合的分层结果**，不能只给一个总分
- 指标：**BLEU-4** + **ROUGE**
- gloss-free 普遍打不过 gloss-based，数字偏低是正常的，**如实报告**
- 一行只允许对应一次真实跑通的实验。**跑挂了也要记，写明失败原因**

## 参考基线（gloss-based，test 集 BLEU-4）

本人于 2026-09-14 核定。**此前 CLAUDE.md 里的「约 29 / 约 25.5」是凭印象写的，
已作废。**

| PHOENIX-2014T | BLEU-4 | | CSL-Daily | BLEU-4 |
|---------------|-------:|---|-----------|-------:|
| TextCTC-SLT | **28.42** | | TwoStream-SLT | **25.79** |
| TwoStream-SLT | 26.71 | | SLTUNET | 23.76 |
| SLTUNET | 26.00 | | TextCTC-SLT | 22.47 |
| ConSLT | 25.48 | | MMTLB | 21.46 |
| MMTLB | 24.60 | | BN-TIN-Transf+BT | 19.67 |

### ⚠️ 这张表不是本项目的对标目标

引用它之前必须同时说清两件事，**缺一件就是误导**：

1. **这些是 gloss-based，本项目是 gloss-free。** 用了 gloss 中间标注的方法
   天然占优，两者不同赛道。
2. **这些跑在 PHOENIX-2014T 和 CSL-Daily 上，本项目跑在 CE-CSL 上。**
   **不同数据集的 BLEU 不可直接比较。**

所以它的作用只是**量级参照**：说明"这个任务上 20-28 就是当前最好水平"，
用来说明本项目的数字该被放在什么尺度上读 —— **不是**说明本项目离 SOTA 差多少。

**把自己的 CE-CSL 数字和 28.42 并排写出来，是这个项目最容易犯的严重错误。**
真被问"你比 SOTA 差多少"，正确答法是：不可比，并说明上面两条原因。

CE-CSL 本身**没有可直接对标的公开 SOTA**（见 DECISIONS.md D-001）。
本项目唯一真正成立的比较是**表内四行之间的纵向比较**。

## 消融表

| 实验编号 | 阶段 | 改了什么 | BLEU-4 | ROUGE | 训练耗时 | 备注 |
|---------|------|---------|--------|-------|---------|------|
| — | — | *（尚无实验，数据预处理进行中）* | — | — | — | — |

### 计划中的四行（跑完后替换上表）

| 实验编号 | 阶段 | 改了什么 | BLEU-4 | ROUGE | 训练耗时 | 备注 |
|---------|------|---------|--------|-------|---------|------|
| E-000 | 0 | 老架构 baseline：逐帧 CNN 特征 + LSTM encoder-decoder seq2seq | | | | 对照组，复刻六年前架构 |
| E-001 | 1 | 只换视觉编码器：CNN → 冻结预训练时空编码器，解码器不动 | | | | 单变量 |
| E-002 | 2 | 只换解码器：LSTM → LLM + 可训练投影层，视觉端不动 | | | | 单变量 |
| E-003 | 3 | 加 pose 第二条流 + 跨模态融合 | | | | 完整架构 |

**纪律**：不要一步到位替换。每阶段只动一个变量，否则消融表失去解释力。

## 每行必须能回答

填一行的时候，确认自己能答出下面这些，答不出就说明这次实验没做完：

1. 相对上一行，**只**改了这一件事吗？还是顺手改了别的（学习率、batch、
   数据增强）？顺手改的必须写进「改了什么」。
2. 这个数字是在 **test** 上跑的吗？用的是哪个 checkpoint（best dev？last？）
3. BLEU-4 用的哪个实现？tokenize 方式是什么（中文按字还是按词）？
   **不同实现之间差几个点是常事，横向比较前必须对齐。**
4. 训练耗时包含数据加载吗？单卡还是多卡？
5. 跑了几个 seed？如果只有 1 个，**不要用"提升了 X 个点"这种说法**。

## 实验日志明细

> 每个实验在这里补充：超参、曲线观察、异常现象、失败尝试。
> 表格给结论，这里给过程。面试被追问细节时看这里。

### E-000（未开始）

